[
    {
      "name": "${container_name}",
      "image": "${image}",
      "cpu": ${container_cpu},
      "memory": ${container_memory},
      "networkMode": "${network_mode}",
      "essential": ${essential},
      "portMappings": [
        {
          "containerPort": ${container_port},
          "hostPort": ${host_port}
        }
      ]
    }
]